import java.io.File;
import java.nio.file.*;
import java.util.ArrayList;

public class DataIntegrityChecker {
    private ArrayList array = new ArrayList<>();
    public void check(ArrayList data){
        /**
        if(array.size()<30){
            array.add(data);
        } else{
            array.remove(0);
            array.add(data);
        }
        */
        System.out.println(data);
        
    }
    
    
}
